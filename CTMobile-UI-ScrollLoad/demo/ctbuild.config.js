const path = require('path');

module.exports = {
  getConfig(config) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctmobile/ui-scrollload': path.resolve(__dirname, 'src/ctmobile-ui-scrollload'),
      //   },
      // },
    };
  },
};
