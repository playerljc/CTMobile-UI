const path = require('path');

module.exports = {
  getConfig(config) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctmobile/ui-listview': path.resolve(__dirname,'src/ctmobile-ui-listview'),
      //   }
      // }
    };
  },
};
