const path = require('path');

module.exports = {
  getConfig(webpack) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctmobile/ui-listview': path.resolve(__dirname,'src/ctmobile-ui-listview'),
      //   }
      // }
    };
  }
};