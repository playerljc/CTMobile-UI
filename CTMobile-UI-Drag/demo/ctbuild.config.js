const path = require('path');

module.exports = {
  getConfig(config) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctmobile/ui-drag': path.resolve(__dirname, 'src/ctmobile-ui-drag'),
      //   },
      // },
    };
  },
};
