const path = require('path');

module.exports = {
  getConfig(config) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctmobile/ui-draglayout': path.resolve(__dirname,'src/ctmobile-ui-draglayout'),
      //   }
      // }
    };
  },
};
