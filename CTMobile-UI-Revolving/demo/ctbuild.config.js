const path = require('path');

module.exports = {
  getConfig(webpack) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctmobile/ui-revolving': path.resolve(__dirname, 'src/ctmobile-ui-revolving'),
      //   },
      // },
    };
  },
};
