const path = require('path');

module.exports = {
  getConfig(webpack) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctmobile/ui-jdcategorytab': path.resolve(__dirname, 'src/ctmobile-ui-jdcategorytab'),
      //   }
      // }
    };
  }
};