const path = require('path');

module.exports = {
  getConfig(config) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctmobile/ui-imageviewer': path.resolve(__dirname, 'src/ctmobile-ui-imageviewer'),
      //   }
      // }
    };
  },
};
