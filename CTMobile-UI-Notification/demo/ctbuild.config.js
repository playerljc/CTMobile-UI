const path = require('path');

module.exports = {
  getConfig(webpack) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctmobile/ui-notification': path.resolve(__dirname, 'src/ctmobile-ui-notification'),
      //   },
      // },
    };
  },
};
