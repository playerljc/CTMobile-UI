const path = require('path');

module.exports = {
  getConfig(config) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctmobile/ui-form': path.resolve(__dirname, 'src/ctmobile-ui-form'),
      //   }
      // }
    };
  },
};
