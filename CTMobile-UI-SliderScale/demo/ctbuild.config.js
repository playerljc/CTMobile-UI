const path = require('path');

module.exports = {
  getConfig({ plugins }) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctmobile/ui-sliderscale': path.resolve(__dirname, 'src/ctmobile-ui-sliderscale'),
      //   },
      // },
    };
  },
};
