const path = require('path');

module.exports = {
  getConfig(webpack) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctmobile/ui-kwlist': path.resolve(__dirname,'src/ctmobile-ui-kwlist'),
      //   }
      // }
    };
  },
};
