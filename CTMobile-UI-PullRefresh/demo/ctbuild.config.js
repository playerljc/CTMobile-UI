const path = require('path');

module.exports = {
  getConfig(config) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctmobile/ui-pullrefresh': path.resolve(__dirname, 'src/ctmobile-ui-pullrefresh'),
      //   },
      // },
    };
  },
};
